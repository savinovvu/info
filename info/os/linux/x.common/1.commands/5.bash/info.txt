# вернуть код возврата
echo $?

# вторая команда выполнится, если первая выполнится корректнро
ls -l && echo OK

# вывести если первая команда завершится с ошибкой
ls -l || echo ERR


# не имеет значения, как завершится первая команда, вторая команда выполнится
ls -l ; echo "No ok"
